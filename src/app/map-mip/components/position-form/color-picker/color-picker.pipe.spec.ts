/* tslint:disable:no-unused-variable */

import { ColorPickerPipe } from './color-picker.pipe';

describe('ColorPickerPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorPickerPipe();
    expect(pipe).toBeTruthy();
  });
});
